1. Put *nix files into /etc/nixos/
2. Add necessary files to configuration.nix
example:
    imports =
    [ # Include the results of the hardware scan.
      ./hardware-configuration.nix
      ./Radioactive.nix
      ./We10X-special.nix
    ];

3. sudo nixos-rebuild swith
4. Select theme in settings
