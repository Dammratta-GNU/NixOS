1. Add necessary files to configuration.nix
example:
    imports =
    [ # Include the results of the hardware scan.
      ./hardware-configuration.nix
      ./Radioactive.nix
    ];

2. sudo nixos-rebuild swith
3. Select theme in settings
